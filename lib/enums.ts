export enum DoctorType {
  referring,
  consulting,
}

export enum Days {
  Monday,
  Tuesday,
  Wednesday,
  Thursday,
  Friday,
  Saturday,
  Sunday,
}

export enum Actions {
  VIEW,
  UPDATE,
  CREATE,
  DELETE,
  PRINT,
}

export enum Modules {
  USER,
  DOCTOR_MASTER,
}
